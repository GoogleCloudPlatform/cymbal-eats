import { Container, Node, Comment } from 'postcss';
import { ControlDirective } from '../@types';
declare type WalkContainerControlDirectiveCallback = (comment: Comment, controlDirective: ControlDirective) => void;
declare type WalkContainerNodeCallback = (node: Node) => void;
export declare const walkContainer: (container: Container, filter: string[], directiveCallback: WalkContainerControlDirectiveCallback, nodeCallback: WalkContainerNodeCallback) => void;
export {};
