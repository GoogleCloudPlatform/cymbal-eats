import { Rule } from 'postcss';
import { strings } from '../@types';
export declare const addSelectorPrefixes: (rule: Rule, prefixes: strings) => void;
export declare const hasSelectorsPrefixed: (rule: Rule) => boolean;
export declare const addProperSelectorPrefixes: (ruleFlipped: Rule, ruleFlippedSecond: Rule, ruleBoth: Rule, ruleSafe: Rule) => void;
